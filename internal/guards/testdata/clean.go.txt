package fixture

import "encoding/json"

// The only way to produce JSON text.
func marshalled(sha string, n int) ([]byte, error) {
	return json.Marshal(map[string]any{"after": sha, "number": n})
}

// A constant document needs no encoder.
func constant() string {
	return `{"conclusion":null,"number":1}`
}
